class ClassModel {
  int id;
  String designation;
  String description;
  String pictureUrl;
  bool isActive;
  int duration;
  String languageCode;

  ClassModel(this.id, this.designation, this.description, this.pictureUrl,
      this.isActive, this.duration, this.languageCode);
}
